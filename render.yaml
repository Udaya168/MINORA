services:
  - type: web
    name: minora-storefront
    env: static
    buildCommand: npm run build
    staticPublishPath: .output/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
